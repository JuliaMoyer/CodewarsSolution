https://www.codewars.com/kata/5558cc216a7a231ac9000022/train/javascript

function duplicates(arr) {
  let duplUnique = arr.filter((el, i) => arr.indexOf(el) !== arr.lastIndexOf(el) && i !== arr.indexOf(el));
  let arr2 = duplUnique.filter((el,i) =>  i === duplUnique.indexOf(el))
  return arr2;
}
